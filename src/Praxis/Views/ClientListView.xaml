<UserControl x:Class="Praxis.Views.ClientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Praxis.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ClientListViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Clients" Style="{StaticResource HeaderLarge}" />
            <TextBlock Text="Your active clients" Style="{StaticResource TextMuted}" Margin="0,4,0,0" />
        </StackPanel>
        
        <!-- Search and Actions -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                     Style="{StaticResource TextInput}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,12,0">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            
            <Button Grid.Column="1" 
                    Content="+ New Client" 
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding NewClientCommand}" />
        </Grid>
        
        <!-- Client List -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <ListBox ItemsSource="{Binding Clients}"
                     SelectedItem="{Binding SelectedClient}"
                     BorderThickness="0"
                     Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListItem}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding PreferredName}" 
                                               Style="{StaticResource HeaderSmall}" />
                                    <TextBlock Text="{Binding PrimaryFocus}" 
                                               Style="{StaticResource TextMuted}"
                                               TextTrimming="CharacterEllipsis"
                                               MaxWidth="400" />
                                </StackPanel>
                                
                                <TextBlock Grid.Column="1" 
                                           Text="{Binding Pronouns}" 
                                           Style="{StaticResource TextMuted}"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding OpenClientCommand}" 
                                CommandParameter="{Binding SelectedClient}" />
                </ListBox.InputBindings>
            </ListBox>
        </Border>
        
        <!-- Empty State -->
        <StackPanel Grid.Row="2" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Visibility="{Binding Clients.Count, Converter={x:Null}}">
            <TextBlock Text="No clients yet" 
                       Style="{StaticResource HeaderMedium}"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextMuted}" />
            <TextBlock Text="Add your first client to get started" 
                       Style="{StaticResource TextMuted}"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
